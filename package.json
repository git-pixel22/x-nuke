{
  "name": "x-nuke",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Delete every post, reply and repost on your own X account, from the browser console.",
  "scripts": {
    "predev": "node scripts/sync-script.js",
    "dev": "vite",
    "prebuild": "node scripts/sync-script.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
